// eslint-disable-next-line
function CardStatBox({ heading, value }) {
  return (
    <div className="">
      <span className="text-white text-[14px] block">{heading}</span>
      <span className="text-white text[14px] ">{value}</span>
    </div>
  );
}

export default CardStatBox;
